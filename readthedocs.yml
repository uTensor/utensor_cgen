version: 2

build:
    image: latest

python:
    version: 3.6
    install:
      - method: pip
      - path: .
      - extra_requirements:

sphinx:
  builder: html
  configuration: doc/source/conf.py